# Archived Next App

`apps/web` is retained only as historical reference. It is not the active F1 InsightX UI and should not be used for local development, deployment, or product work.

Use the root TanStack Start app instead:

```sh
npm.cmd run dev
```

Local URL:

```text
http://127.0.0.1:8080
```
