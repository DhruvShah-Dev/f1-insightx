console.error(
  [
    "apps/web is the archived Next.js UI and is no longer the active F1 InsightX app.",
    "Use the root TanStack Start app instead:",
    "  npm.cmd run dev",
    "  http://127.0.0.1:8080",
  ].join("\n"),
);
process.exit(1);
